// Canonical form input style — use this for all text/date/number/select/textarea inputs
export const inputClass =
  'border border-blue-grey-200 rounded-xl px-3 py-2 text-base focus:outline-none focus:ring-2 focus:ring-twilight-indigo-300 dark:bg-navy-600 dark:border-navy-500 dark:text-navy-50 dark:placeholder:text-navy-200 dark:focus:ring-sky-500';

// Base panel visuals — use when spacing/layout needs to differ from default
export const panelBase =
  'bg-white rounded-2xl border border-blue-grey-100 dark:bg-navy-700 dark:border-navy-600';

// Canonical card/panel style — use this for all content panels and widgets
export const panelClass = `${panelBase} p-5 flex flex-col gap-3`;
